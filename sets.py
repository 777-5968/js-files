#sets are unordered,mutable and do not allow duplicates
myclasses = {"chatgpt","gemini","google","chrome"}
print(myclasses)
print(type(myclasses))
#len
print(len(myclasses))
#loop
for y in myclasses:
    print(y)
